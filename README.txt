.gitignore for node_modules

#Comands used in git bash

    · "npm init -y": initiañices npm by creating a package.json  w/ version number, licence, description,
and updates when using dependencies

    · "npm i express": installs express

    · "node index.js": runs index.js